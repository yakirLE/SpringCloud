spring:
  application:
    name: config-client-app
  cloud:    config:
      # "Config First" method: 
      #uri: http://localhost:8888/
      
      # "Discovery First" method:      discovery:        enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka